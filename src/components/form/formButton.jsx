import React from "react";

function FormButton({ onSubmitEventHandler }) {
    return (
        <button type="submit" className="form__button">Tambah</button>
    );
}

export default FormButton;
